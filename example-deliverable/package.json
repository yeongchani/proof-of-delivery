{
  "name": "@pod/example-deliverable",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run --reporter=default --reporter=json --outputFile=test-results.json"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  }
}
